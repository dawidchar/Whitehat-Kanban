<div class="" style="width: 3.3rem; height:3.3rem;" tippy="true" title="{{name}}">
    <div id="user-{{userid}}" draggable="${(readonly) ? 'false' : 'true'}" ondragstart="app.run('dragStart', event)"
        class="imageicon rounded-circle shadow ${(readonly) ? '' : 'grabbable'}" style="background-image:url('{{avatar}}');"></div>
</div>